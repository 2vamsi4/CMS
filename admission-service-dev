spring:
  profiles: dev

  datasource:
    url: jdbc:mysql://localhost:3306/admission_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root

server:
  port: 8086

logging:
  level:
    root: DEBUG
