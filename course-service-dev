spring:
  profiles: dev

  datasource:
    url: jdbc:mysql://localhost:3306/courses_db?useSSL=false&serverTimezone=UTC
    username: root
    password: root

server:
  port: 8088

logging:
  level:
    root: DEBUG
